# Build artifacts
build/
build_pkg/
dist/
*.spec
__pycache__/
*.pyc
*.pyo

# Phase 4: WAV originals stay local (re-bake source). Repo + exe ship .ogg only.
assets/audio/**/*.wav
assets/audio/*.wav

# Virtualenvs
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local-only secrets (none should exist here, but defensive)
.env
.env.*

# DO NOT IGNORE — kept in repo per CLAUDE.md GUI Integrity Rule:
# !assets/
# !lessons/
# !sandbox_ai/
# !icons/
# !parent/
# !vendor/
voices/

# Audio QA reports — locally generated, contain timestamps + sampled paths
audio_qa/
